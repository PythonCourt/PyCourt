# 配置指南

本页将介绍 PyCourt 相关的配置文件与约定：

- `pyproject.toml` 中的 `[tool.pycourt]`；
- `pycourt/yaml/config.yaml`（法律与家族配置）；
- `pycourt.yaml`（治外法权与项目级覆盖）；
- `tools/pycourt/config/read_toml.py` 如何为 CI 提供统一视图。

> 当前为占位文档，后续可以按实际结构逐步补充示例和推荐实践。
